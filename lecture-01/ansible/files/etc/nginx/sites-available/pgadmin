server {

	listen 80;
	root /var/www/html;
	server_name pgad.wa.its;

        access_log /var/log/nginx/pgadmin-access.log lb;
        error_log /var/log/nginx/pgadmin-01-error.log;

	location / {
		proxy_pass http://localhost:8080;
                proxy_set_header Host $host;
	}
}

